stages:

- stage: Build
  jobs:
  - job: Mac
    pool: 
      vmImage: macOS-latest
    steps:
    - powershell: Write-Host "##vso[task.setvariable variable=MacVariable;]HelloMac"
      displayName: Set Variable

  - job: Windows
    dependsOn: Mac
    pool:
      vmImage: windows-latest
    steps:
    - powershell: Write-Host $(MacVariable)
      displayName: Get Variable
